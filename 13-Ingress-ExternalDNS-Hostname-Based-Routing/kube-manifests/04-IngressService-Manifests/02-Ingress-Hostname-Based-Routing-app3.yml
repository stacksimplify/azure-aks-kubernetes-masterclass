apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-app3
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: capp3.kubeoncloud.com    
    http:
      paths:
      - path: /
        backend:
          serviceName: usermgmt-webapp-clusterip-service
          servicePort: 80                        